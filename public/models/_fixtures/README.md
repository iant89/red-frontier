# Test fixtures

`placeholder.glb` is a minimal valid glTF 2.0 binary (one triangle, ~0.6 KB).
It exists so the GLB pipeline can be tested without Leonardo art. Do not replace
it with production models.
